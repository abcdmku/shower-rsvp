import type { Meta } from '@storybook/react';
import { ViewSongs } from './ViewSongs';

const Story: Meta<typeof ViewSongs> = {
  component: ViewSongs,
  title: 'ViewSongs',
};
export default Story;

export const Primary = {
  args: {},
};
