import type { Meta } from '@storybook/react';
import { MainForm } from './MainForm';

const Story: Meta<typeof MainForm> = {
  component: MainForm,
  title: 'MainForm',
};
export default Story;

export const Primary = {
  args: {},
};
