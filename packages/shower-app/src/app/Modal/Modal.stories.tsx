import type { Meta } from '@storybook/react';
import { ModalForm } from './Modal';

const Story: Meta<typeof ModalForm> = {
  component: ModalForm,
  title: 'ModalForm',
};
export default Story;

export const Primary = {
  args: {},
};
