import type { Meta } from '@storybook/react';
import { ModalForm3 } from './Modal';

const Story: Meta<typeof ModalForm3> = {
  component: ModalForm3,
  title: 'ModalForm3',
};
export default Story;

export const Primary = {
  args: {},
};
