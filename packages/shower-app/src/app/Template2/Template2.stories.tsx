import type { Meta } from '@storybook/react';
import { Template2 } from './Template2';

const Story: Meta<typeof Template2> = {
  component: Template2,
  title: 'Template2',
};
export default Story;

export const Primary = {
  args: {},
};
